import{expect,test}from"@playwright/test";test("shows explainable duplicate suggestion without auto merge",async({page})=>{await page.goto("/jobs/00000000-0000-4000-8000-000000000001");await expect(page.getByRole("heading",{name:"유사 공고 후보"})).toBeVisible();await expect(page.getByText("유사도 90%")).toBeVisible();await expect(page.getByRole("button",{name:"같은 공고"})).toBeVisible();await expect(page.getByRole("button",{name:"별개로 유지"})).toBeVisible();});
